<%= render 'shared/index', list: @users, show_columns: ['id', 'name', 'email', 'userable_type',
  'provider', 'sign_in_count', 'current_sign_in_at', 'current_sign_in_ip', 'last_sign_in_at', 'last_sign_in_ip'] do %>
  <% if can? :create, User %>
    <%= link_to new_user_path, class: 'btn btn-xs btn-primary' do %>
      <i class="fa fa-plus"></i> Cadastrar Usuário
    <% end %>
  <% end %>
<% end %>
