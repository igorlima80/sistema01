<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : @app_name %></title>
  <%= csrf_meta_tags %>

  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
  <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
  <![endif]-->

  <%= stylesheet_link_tag "application", :media => "all" %>

  <!-- For all other devices -->
  <!-- Size should be 32 x 32 pixels -->
  <%#= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
  <!-- Google Font -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,600,700,300italic,400italic,600italic">

</head>
<body class="skin-blue-light login-page">
<div id="RAILS_ENV_CONSTANT" style="display:none"><%= Rails.env %></div>
  <section class="content">
    <div class="login-box">
      <div class="row">
        <div class="col-md-12">
          <!-- Widget: user widget style 1 -->
          <div class="box box-widget widget-user-2">
            <!-- Add the bg color to the header using any of the bg-* classes -->
            <div class="widget-user-header bg-blue" >
              <div class="widget-user-image">
                <%= image_tag 'logoBH.png', style: 'width: 100px !important' %>               
              </div>
              <br /><br />
              <!-- /.widget-user-image -->
              <h3 class="widget-user-username"><%= @app_name %></h3>
              <h5 class="widget-user-desc"><%= @app_description %></h5>
            </div>
            <div class="box-body">
              <%= bootstrap_flash %>
              <%= yield %>
            </div>
          </div>
          <!-- /.widget-user -->
        </div>
      </div>
    </div>
  </section>

  <%= javascript_include_tag "application" %>
</body>
</html>
